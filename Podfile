# Uncomment the next line to define a global platform for your project
  platform :ios, '13.0'

target 'NeteaseMusic Comment' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for NeteaseMusic Comment
  pod 'SnapKit'
  pod 'Moya'
  pod 'iOSPalette'
  pod 'SDWebImage'
  
  target 'NeteaseMusic CommentTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'NeteaseMusic CommentUITests' do
  inherit! :search_paths
    # Pods for testing
  end

end
